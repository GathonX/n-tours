"use client";
import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function Loading(){

    return (
        <Card>
            <CardHeader>
                <CardTitle>
                    Error Warning Error
                </CardTitle>
            </CardHeader>
        </Card>
    )
}